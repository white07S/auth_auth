import React from 'react';

const Landing = () => (
  <section className="page">
    <h1>Welcome to the Auth BFF demo</h1>
    <p>This UI keeps tokens off the client and relies on the FastAPI backend for authentication.</p>
    <p>Use the navigation to explore protected routes once you are signed in.</p>
  </section>
);

export default Landing;

